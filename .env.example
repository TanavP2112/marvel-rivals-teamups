# Copy this file to .env.local and fill in your own Supabase project
# details once the backend is ready. Until these are set, src/lib/supabase.ts
# exports `undefined` and the app keeps using localStorage for votes.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
